# =============================================================================
# SCRAPPLAYERV6 Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=5000

# -----------------------------------------------------------------------------
# Session Security (REQUIRED)
# -----------------------------------------------------------------------------
# Generate a strong random secret for session encryption
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARACTERS

# Session configuration
SESSION_MAX_AGE=86400000  # 24 hours in milliseconds
SESSION_NAME=connect.sid  # Session cookie name

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL for main app data (tracks, posts, etc.)
DATABASE_URL=postgresql://user:password@localhost:5432/scrapplayer

# SQLite auth database is stored in ./data/auth.db (created automatically)

# -----------------------------------------------------------------------------
# HTTPS Configuration (Production)
# -----------------------------------------------------------------------------
# For local development with HTTPS (optional):
# HTTPS_ENABLED=true
# HTTPS_KEY_PATH=/path/to/private-key.pem
# HTTPS_CERT_PATH=/path/to/certificate.pem

# For production, use a reverse proxy (nginx, Caddy) or platform-managed TLS
# Examples:
# - Nginx reverse proxy with Let's Encrypt
# - Caddy (automatic HTTPS)
# - Platform services (Heroku, Railway, Render, etc.)

# -----------------------------------------------------------------------------
# YouTube Integration
# -----------------------------------------------------------------------------
# Optional: YouTube Data API for "For You" recommendations
# GOOGLE_API_KEY=your_youtube_api_key_here

# -----------------------------------------------------------------------------
# Object Storage (Optional)
# -----------------------------------------------------------------------------
# For production file persistence
# AUDIO_STORAGE_DIR=path/to/storage
# PRIVATE_OBJECT_DIR=path/to/private/storage

# -----------------------------------------------------------------------------
# Security & Rate Limiting
# -----------------------------------------------------------------------------
# CORS allowed origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:5000,https://yourdomain.com

# Rate limiting is configured in code - see server/middleware/rateLimit.ts

# -----------------------------------------------------------------------------
# Cleanup Job Configuration
# -----------------------------------------------------------------------------
# Hour of day (0-23) to run account cleanup job
# Default: 3 (3 AM)
CLEANUP_HOUR=3

# -----------------------------------------------------------------------------
# IMPORTANT SECURITY NOTES
# -----------------------------------------------------------------------------
# 1. SESSION_SECRET MUST be changed from default
# 2. Run behind HTTPS in production (never HTTP)
# 3. Keep this file secure - it contains sensitive credentials
# 4. Rotate SESSION_SECRET periodically (invalidates all sessions)
# 5. No password recovery - lost passwords = permanent account loss
# 6. Accounts inactive for 7+ days are automatically deleted
